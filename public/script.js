// // io() connects to the sock.io server at the URL specified in the argument
// const socket = io("http://localhost:4000");

// // like on the server, our socket has an on method and emit method
// socket.on("welcome", (data) => {
//   console.log(data);
//   socket.emit("thanks", ["hello", "world", "friends", "we are connected!"]);
// });
